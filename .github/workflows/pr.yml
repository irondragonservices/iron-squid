# Caller. All the logic lives in irondragonservices/.github; this file only
# says which upstream defines the version.
name: pull request

on:
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    uses: irondragonservices/.github/.github/workflows/image-pr.yml@3ab64c35ed5d357c0b1285e23060035440a8d8e6 # main
    with:
      version-arg: SQUID_VERSION
